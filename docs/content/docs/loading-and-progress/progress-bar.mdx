---
title: Progress Bar
description: Show current progress.
---

import { ProgressBarStory } from "@/components/stories/progress-bar.story";

<ProgressBarStory />

## Usage

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/progress-bar";
```

```html
<ore-progress-bar value="65" max="100" variant="labeled"></ore-progress-bar>
```

    </Tab>
    <Tab value="React">

```tsx
import { ProgressBar } from "@oreui-web/react/progress-bar";

export function DownloadProgress() {
  return <ProgressBar value={65} max={100} variant="labeled" />;
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ProgressBar } from "@oreui-web/vue/progress-bar";
</script>

<template>
  <ProgressBar :value="65" :max="100" variant="labeled" />
</template>
```

    </Tab>
    <Tab value="Astro">

```astro
<ore-progress-bar value="65" max="100" variant="labeled"></ore-progress-bar>

<script>
	import "oreui-web/progress-bar";
</script>
```

    </Tab>

</Tabs>

## Labels

The labeled variant shows a percentage by default. Set `label` to display custom text.

```html
<ore-progress-bar
  value="65"
  variant="labeled"
  label="Loading resources"
></ore-progress-bar>
<ore-progress-bar
  value="65"
  variant="labeled"
  label-position="top"
  label-align="start"
></ore-progress-bar>
```

Custom label markup can use the public label class:

```html
<ore-progress-bar value="65" variant="labeled">
  <span class="ore-progress-bar-label">65 of 100 chunks</span>
</ore-progress-bar>
```

## API

| Property        | Attribute        | Type                         | Default    |
| --------------- | ---------------- | ---------------------------- | ---------- |
| `label`         | `label`          | `string`                     | `""`       |
| `labelAlign`    | `label-align`    | `"center" \|"end" \|"start"` | `"center"` |
| `labelPosition` | `label-position` | `"bottom" \|"top"`           | `"bottom"` |
| `max`           | `max`            | `number`                     | `100`      |
| `value`         | `value`          | `number`                     | `0`        |
| `variant`       | `variant`        | `"labeled" \|"plain"`        | `"plain"`  |

## Public classes

| Class                     | Purpose                    |
| ------------------------- | -------------------------- |
| `.ore-progress-bar-track` | Progress track and fill    |
| `.ore-progress-bar-label` | Percentage or custom label |

## CSS variables

| Variable                       | Purpose                       |
| ------------------------------ | ----------------------------- |
| `--ore-progress-bar-unit`      | Base size unit                |
| `--ore-progress-bar-frame`     | Frame and empty track color   |
| `--ore-progress-bar-fill`      | Filled track color            |
| `--ore-progress-bar-label`     | Label text color              |
| `--ore-progress-bar-label-gap` | Space between label and track |

## Accessibility

Progress Bar uses `role="progressbar"` and keeps `aria-valuemin`, `aria-valuemax`, and `aria-valuenow` in sync. Custom label is exposed through `aria-valuetext`.
