---
title: Progress Bar 进度条
description: 显示当前进度
---

import { ProgressBarStory } from "@/components/stories/progress-bar.story";

<ProgressBarStory />

## 用法

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/progress-bar";
```

```html
<progress class="ore-progress-bar" value="65" max="100" aria-label="Download progress"></progress>
```

    </Tab>
    <Tab value="React">

```tsx
import { ProgressBar } from "@oreui-web/react/progress-bar";

export function DownloadProgress() {
  return <ProgressBar value={65} max={100} aria-label="Download progress" />;
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ProgressBar } from "@oreui-web/vue/progress-bar";
</script>
<template><ProgressBar :value="65" :max="100" aria-label="Download progress" /></template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import ProgressBar from "@oreui-web/svelte/progress-bar";
</script>

<ProgressBar value={65} max={100} aria-label="Download progress" />
```

    </Tab>
    <Tab value="Solid">

```tsx
import { ProgressBar } from "@oreui-web/solid/progress-bar";

export function LoadingProgress() {
  return <ProgressBar value={64} max={100} aria-label="Loading world" />;
}
```

    </Tab>
    <Tab value="Astro">

```astro
<progress class="ore-progress-bar" value="65" max="100" aria-label="Download progress"></progress>

<script>
	import "oreui-web/progress-bar";
</script>
```

    </Tab>

</Tabs>

## 标签

需要可见标签时，使用相邻的 `output`。将它放在进度条之前或之后即可选择标签位置。

```html
<progress class="ore-progress-bar" value="65" max="100" aria-labelledby="download-label"></progress>
<output class="ore-progress-bar-label" id="download-label">65%</output>

<output class="ore-progress-bar-label" data-align="start" id="chunks-label">65 of 100 chunks</output>
<progress class="ore-progress-bar" value="65" max="100" aria-labelledby="chunks-label"></progress>
```

## API

Progress Bar 接受 HTML `progress` 元素的标准属性，包括 `value` 和 `max`。省略 `value` 可表示不确定进度。

## 公开类

| 类                        | 用途               |
| ------------------------- | ------------------ |
| `.ore-progress-bar`       | 原生进度条元素     |
| `.ore-progress-bar-label` | 可选的可见标签     |

## CSS 变量

| 变量                           | 用途               |
| ------------------------------ | ------------------ |
| `--ore-progress-bar-unit`      | 基础尺寸单位       |
| `--ore-progress-bar-frame`     | 边框和空轨道颜色   |
| `--ore-progress-bar-fill`      | 已填充轨道颜色     |
| `--ore-progress-bar-label`     | 标签文本颜色       |

## 无障碍

原生 `progress` 元素提供进度条语义和值信息。使用 `aria-label` 或 `aria-labelledby` 提供无障碍名称，或者用可见的 `output` 提供该名称。
