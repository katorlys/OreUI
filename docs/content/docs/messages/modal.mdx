---
title: Modal
description: Present content in a modal dialog.
---

import { ModalStory } from "@/components/stories/modal.story";

<ModalStory />

## Usage

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">

```js
import "oreui-web/modal";
```

```html
<button type="button" data-modal-target="form-modal">Open modal</button>

<dialog
  class="ore-modal"
  id="form-modal"
  aria-labelledby="form-modal-title"
  aria-describedby="form-modal-description"
>
  <form class="ore-modal-content" method="dialog">
    <header class="ore-modal-header">
      <button
        class="ore-modal-header-button"
        type="submit"
        value="back"
        aria-label="Back"
      >
        <svg aria-hidden="true" viewBox="0 0 8 8">...</svg>
      </button>
      <h2 class="ore-modal-title" id="form-modal-title">Header</h2>
      <button
        class="ore-modal-header-button"
        type="submit"
        value="close"
        aria-label="Close"
      >
        <svg aria-hidden="true" viewBox="0 0 8 8">...</svg>
      </button>
    </header>
    <div
      class="ore-container modal-form-body ore-scrollbar"
      data-variant="dark"
    >
      <p class="ore-modal-description" id="form-modal-description">
        Add the information for this form.
      </p>
      <div class="ore-textfield">
        <label class="ore-textfield-label" for="modal-label">Label</label>
        <span class="ore-textfield-control">
          <input class="ore-textfield-input" id="modal-label" />
        </span>
        <span class="ore-textfield-description">Description</span>
      </div>
    </div>
    <div class="ore-container modal-confirmation" data-variant="light">
      <label><input type="checkbox" />Checkbox example for confirmation</label>
    </div>
    <div class="ore-container modal-actions" data-variant="light">
      <button type="submit" value="confirm">Confirm</button>
      <button type="submit" value="cancel" data-color="secondary">
        Cancel
      </button>
    </div>
  </form>
</dialog>
```

    </Tab>
    <Tab value="React">

```tsx
import { Modal } from "@oreui-web/react/modal";
import { useRef } from "react";

export function FormModal() {
  const modal = useRef<HTMLDialogElement>(null);

  return (
    <>
      <button type="button" onClick={() => modal.current?.showModal()}>
        Open modal
      </button>
      <Modal ref={modal} aria-labelledby="form-modal-title">
        <form className="ore-modal-content" method="dialog">
          ...
        </form>
      </Modal>
    </>
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ref } from "vue";
import { Modal } from "@oreui-web/vue/modal";
const modal = ref<HTMLDialogElement>();
</script>

<template>
  <button type="button" @click="modal?.showModal()">Open modal</button>
  <Modal ref="modal" aria-labelledby="form-modal-title">
    <form class="ore-modal-content" method="dialog">...</form>
  </Modal>
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import Modal from "@oreui-web/svelte/modal";
  let modal: HTMLDialogElement;
</script>

<button type="button" onclick={() => modal.showModal()}>Open modal</button>
<Modal bind:this={modal} aria-labelledby="form-modal-title">
  <form class="ore-modal-content" method="dialog">...</form>
</Modal>
```

    </Tab>
    <Tab value="Solid">

```tsx
import { Modal } from "@oreui-web/solid/modal";

export function FormModal() {
  let modal: HTMLDialogElement | undefined;
  return (
    <>
      <button type="button" onClick={() => modal?.showModal()}>
        Open modal
      </button>
      <Modal ref={modal} aria-labelledby="form-modal-title">
        <form class="ore-modal-content" method="dialog">
          ...
        </form>
      </Modal>
    </>
  );
}
```

    </Tab>
    <Tab value="Astro">

```astro
<button type="button" data-modal-target="form-modal">Open modal</button>
<dialog class="ore-modal" id="form-modal" aria-labelledby="form-modal-title">
  <form class="ore-modal-content" method="dialog">...</form>
</dialog>

<script>
  import "oreui-web/modal";
</script>
```

    </Tab>

</Tabs>

## Open and close

Call the native `showModal()` method to open a modal dialog. A `form[method="dialog"]` closes it without submitting data; the activated submit button's `value` becomes `dialog.returnValue`.

The optional controller also opens a dialog when a button's `data-modal-target` matches its `id`. Use `data-modal-close="value"` only for a close control that cannot be placed in a dialog form.

```js
const modal = document.querySelector("dialog.ore-modal");
modal.showModal();
modal.close("cancel");
```

## Events

Native `close` fires after the dialog closes, and native `cancel` fires when the user requests dismissal with Escape. Read the result from `returnValue`; the optional controller writes `"backdrop"` or `"escape"` for those dismissal paths.

```js
modal.addEventListener("close", () => {
  console.log(modal.returnValue);
});
```

## Native API

| Member          | Purpose                                        |
| --------------- | ---------------------------------------------- |
| `open`          | Reflects whether the dialog is open            |
| `returnValue`   | Value supplied by `close()` or the dialog form |
| `showModal()`   | Opens in the top layer as a modal              |
| `close(value?)` | Closes with an optional return value           |

## Public classes

| Class                      | Purpose                      |
| -------------------------- | ---------------------------- |
| `.ore-modal`               | Native dialog root           |
| `.ore-modal-content`       | Dialog frame                 |
| `.ore-modal-header`        | Title bar                    |
| `.ore-modal-title`         | Accessible dialog title      |
| `.ore-modal-description`   | Accessible supporting text   |
| `.ore-modal-header-button` | Header action                |
| `.modal-form-body`         | Scrollable dark form section |
| `.modal-confirmation`      | Confirmation section         |
| `.modal-actions`           | Dialog action section        |

## Accessibility

The native dialog provides top-layer rendering, focus containment, inert background content, Escape handling, and focus restoration. Supply `aria-labelledby` and, when useful, `aria-describedby`.
