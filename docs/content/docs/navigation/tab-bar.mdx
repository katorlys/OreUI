---
title: Tab Bar
description: Arrange tab buttons together.
---

import { TabBarStory } from "@/components/stories/tab-bar.story";

<TabBarStory />

## Usage

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/tab-bar";
```

```html
<ore-tab-bar aria-label="Play menu">
  <ore-tab-button selected>Worlds</ore-tab-button>
  <ore-tab-button>Friends</ore-tab-button>
  <ore-tab-button>Servers</ore-tab-button>
</ore-tab-bar>
```

    </Tab>
    <Tab value="React">

```tsx
import { TabBar, TabButton } from "@oreui-web/react/tab-bar";

export function PlayMenu() {
  return (
    <TabBar aria-label="Play menu">
      <TabButton selected>Worlds</TabButton>
      <TabButton>Friends</TabButton>
      <TabButton>Servers</TabButton>
    </TabBar>
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { TabBar, TabButton } from "@oreui-web/vue";
</script>

<template>
  <TabBar aria-label="Play menu">
    <TabButton selected>Worlds</TabButton>
    <TabButton>Friends</TabButton>
    <TabButton>Servers</TabButton>
  </TabBar>
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import TabBar from "@oreui-web/svelte/tab-bar";
  import TabButton from "@oreui-web/svelte/tab-button";
</script>

<TabBar aria-label="Play menu">
  <TabButton selected>Worlds</TabButton>
  <TabButton>Friends</TabButton>
  <TabButton>Servers</TabButton>
</TabBar>
```

    </Tab>
    <Tab value="Solid">

```tsx
import { createSignal } from "solid-js";
import { TabBar, TabButton } from "@oreui-web/solid";

export function NavigationTabs() {
  const [selected, setSelected] = createSignal("play");

  return (
    <TabBar aria-label="Navigation">
      <TabButton
        selected={selected() === "play"}
        onChange={() => setSelected("play")}
      >
        Play
      </TabButton>
      <TabButton
        selected={selected() === "settings"}
        onChange={() => setSelected("settings")}
      >
        Settings
      </TabButton>
    </TabBar>
  );
}
```

    </Tab>
    <Tab value="Astro">

```astro
<ore-tab-bar aria-label="Play menu">
	<ore-tab-button selected>Worlds</ore-tab-button>
	<ore-tab-button>Friends</ore-tab-button>
	<ore-tab-button>Servers</ore-tab-button>
</ore-tab-bar>

<script>
	import "oreui-web/tab-bar";
</script>
```

    </Tab>

</Tabs>

## Selection

Tab Buttons manage selection within their nearest Tab Bar and dispatch `change` when a new tab is selected.

```js
const tabBar = document.querySelector("ore-tab-bar");
tabBar?.addEventListener("change", (event) => {
  const selected = event.target;
  console.log(selected);
});
```

## API

See [Tab Button](../../buttons/tab-button#api).

## Accessibility

Tab Bar uses `role="tablist"`. Add an accessible label and use Tab Button as its direct interactive children.  
For more information, see [Tab Button](../../buttons/tab-button#accessibility).
