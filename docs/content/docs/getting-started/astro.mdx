---
title: Astro
description: Use OreUI in Astro.
---

## Install

```bash tab="pnpm"
pnpm add oreui-web
```

```bash tab="yarn"
yarn add oreui-web
```

```bash tab="npm"
npm install oreui-web
```

```bash tab="bun"
bun add oreui-web
```

## Use a component

Import styles in a shared layout:

```astro title="src/layouts/Layout.astro"
---
import "oreui-web/styles.css";
---

<slot />
```

Register components in client script:

```astro title="src/pages/index.astro"
<ore-button type="button">Create new world</ore-button>

<script>
  import "oreui-web/button";
</script>
```

<Callout type="info">To use all the components, import `oreui-web`.</Callout>

## Examples

Forms and events:

```astro title="src/pages/account.astro"
<form id="account">
  <ore-checkbox name="terms" required>Accept terms</ore-checkbox>
  <ore-button type="submit">Continue</ore-button>
</form>

<script>
  import "oreui-web/button";
  import "oreui-web/checkbox";

  const form = document.querySelector<HTMLFormElement>("#account");

  form?.addEventListener("submit", (event) => {
    event.preventDefault();
    console.log(new FormData(form).get("terms"));
  });
</script>
```

<Callout type="warn">`is:inline` cannot bundle package imports.</Callout>
