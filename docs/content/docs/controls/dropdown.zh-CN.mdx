---
title: Dropdown 下拉菜单
description: 从菜单中选择一个选项
---

import { DropdownStory } from "@/components/stories/dropdown.story";

<DropdownStory />

## 用法

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/button";
import "oreui-web/dropdown";
```

```html
<ore-dropdown variant="bordered" value="creative">
  <ore-button class="ore-dropdown-trigger" type="button" color="secondary">
    <span class="ore-dropdown-trigger-label">Game mode</span>
  </ore-button>
  <div class="ore-dropdown-menu">
    <button class="ore-dropdown-item" type="button" data-value="survival">
      Survival
    </button>
    <button class="ore-dropdown-item" type="button" data-value="creative">
      Creative
    </button>
    <button class="ore-dropdown-item" type="button" data-value="adventure">
      Adventure
    </button>
    <button
      class="ore-dropdown-item"
      type="button"
      data-value="spectator"
      disabled
    >
      Spectator
    </button>
  </div>
</ore-dropdown>
```

    </Tab>
    <Tab value="React">

```tsx
import { Button } from "@oreui-web/react/button";
import { Dropdown } from "@oreui-web/react/dropdown";
import { useState } from "react";

export function GameModeDropdown() {
  const [value, setValue] = useState("creative");

  return (
    <Dropdown
      value={value}
      variant="bordered"
      onChange={(event) => setValue(event.detail.value)}
    >
      <Button className="ore-dropdown-trigger" type="button" color="secondary">
        <span className="ore-dropdown-trigger-label">Game mode</span>
      </Button>
      <div className="ore-dropdown-menu">
        <button
          className="ore-dropdown-item"
          type="button"
          data-value="survival"
        >
          Survival
        </button>
        <button
          className="ore-dropdown-item"
          type="button"
          data-value="creative"
        >
          Creative
        </button>
        <button
          className="ore-dropdown-item"
          type="button"
          data-value="adventure"
        >
          Adventure
        </button>
      </div>
    </Dropdown>
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ref } from "vue";
import { Button, Dropdown } from "@oreui-web/vue";
const value = ref("creative");
</script>

<template>
  <Dropdown v-model="value" variant="bordered">
    <Button class="ore-dropdown-trigger" type="button" color="secondary">
      <span class="ore-dropdown-trigger-label">Game mode</span>
    </Button>
    <div class="ore-dropdown-menu">
      <button class="ore-dropdown-item" type="button" data-value="survival">
        Survival</button
      ><button class="ore-dropdown-item" type="button" data-value="creative">
        Creative
      </button>
      <button class="ore-dropdown-item" type="button" data-value="adventure">
        Adventure
      </button>
      <button
        class="ore-dropdown-item"
        type="button"
        data-value="spectator"
        disabled
      >
        Spectator
      </button>
    </div>
  </Dropdown>
</template>
```

    </Tab>
    <Tab value="Astro">

```astro
<ore-dropdown variant="bordered" value="creative">
  <ore-button class="ore-dropdown-trigger" type="button" color="secondary">
		<span class="ore-dropdown-trigger-label">Game mode</span>
	</ore-button>
	<div class="ore-dropdown-menu">
		<button class="ore-dropdown-item" type="button" data-value="survival">Survival</button>
		<button class="ore-dropdown-item" type="button" data-value="creative">Creative</button>
		<button class="ore-dropdown-item" type="button" data-value="adventure">Adventure</button>
	</div>
</ore-dropdown>

<script>
	import "oreui-web/button";
	import "oreui-web/dropdown";
</script>
```

    </Tab>

</Tabs>

## 变体

`borderless` 只在交互时才显示选项边框，而 `bordered` 会始终显示边框。

## 事件

| 事件          | 详情                                   |
| ------------- | -------------------------------------- |
| `change`      | `{ item: HTMLElement; value: string }` |
| `open-change` | `boolean`                              |

对于 React，请使用 `onChange` 和 `onOpenChange`。

## API

| Prop          | 属性           | 类型                         | 默认值         |
| ------------- | -------------- | ---------------------------- | -------------- |
| `defaultOpen` | `default-open` | `boolean`                    | `false`        |
| `open`        | `open`         | `boolean`                    | `false`        |
| `value`       | `value`        | `string`                     | `""`           |
| `variant`     | `variant`      | `"bordered" \| "borderless"` | `"borderless"` |

## 公开类

| 类                            | 用途                     |
| ----------------------------- | ------------------------ |
| `.ore-dropdown-trigger`       | 打开菜单的按钮           |
| `.ore-dropdown-trigger-label` | 根据所选菜单项更新的文本 |
| `.ore-dropdown-menu`          | 弹出菜单                 |
| `.ore-dropdown-item`          | 可选择的菜单项           |

## CSS 变量

菜单的可滚动区域也可使用所有的 [Scrollbar CSS 变量](../../navigation/scrollbar#css-变量)。

| 变量                                   | 用途                   |
| -------------------------------------- | ---------------------- |
| `--ore-dropdown-unit`                  | 基础尺寸单位           |
| `--ore-dropdown-menu-background`       | 菜单背景颜色           |
| `--ore-dropdown-menu-outline`          | 菜单轮廓颜色           |
| `--ore-dropdown-item-background`       | 菜单项背景             |
| `--ore-dropdown-item-background-hover` | 菜单项悬停背景         |
| `--ore-dropdown-item-border`           | 菜单项之间的分隔线颜色 |
| `--ore-dropdown-foreground`            | 菜单项文本和图标颜色   |
| `--ore-dropdown-visible-items`         | 滚动前可见的菜单项数量 |

## 无障碍

Dropdown 管理菜单角色、选中状态和焦点，支持`方向键`、`Home`、`End`、`Enter`、空格键、`Esc` 和 `Tab` 键。  
对于不可用的菜单项，请使用原生 `disabled` 属性。
