---
title: Slider
description: Select a numeric value from a range.
---

import { SliderStory } from "@/components/stories/slider.story";

<SliderStory />

## Usage

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/slider";
```

```html
<input
  type="range"
  name="distance"
  min="2"
  max="32"
  step="1"
  value="12"
  aria-label="Render distance"
/>
```

    </Tab>
    <Tab value="React">

```tsx
import { Slider } from "@oreui-web/react/slider";
import { useState } from "react";

export function RenderDistanceSlider() {
  const [value, setValue] = useState(12);

  return (
    <Slider
      aria-label="Render distance"
      min={2}
      max={32}
      step={1}
      value={value}
      onInput={(event) => {
        setValue(event.currentTarget.valueAsNumber);
      }}
    />
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ref } from "vue";
import { Slider } from "@oreui-web/vue/slider";

const value = ref(12);
</script>

<template>
  <Slider
    v-model="value"
    name="distance"
    aria-label="Render distance"
    :min="2"
    :max="32"
    :step="1"
  />
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import Slider from "@oreui-web/svelte/slider";

  let value = $state(12);
</script>

<Slider
  bind:value
  name="distance"
  min={2}
  max={32}
  step={1}
  aria-label="Render distance"
/>
```

    </Tab>
    <Tab value="Solid">

```tsx
import { createSignal } from "solid-js";
import { Slider } from "@oreui-web/solid/slider";

export function RenderDistanceSlider() {
  const [value, setValue] = createSignal(12);

  return (
    <Slider
      value={value()}
      onValueChange={setValue}
      name="distance"
      min={2}
      max={32}
      step={1}
      aria-label="Render distance"
    />
  );
}
```

    </Tab>
    <Tab value="Astro">

```astro
<input type="range" name="distance" min="2" max="32" step="1" value="12" aria-label="Render distance" />

<script>
	import "oreui-web/slider";
</script>
```

    </Tab>

</Tabs>

## Variants

```html
<input type="range" value="40" aria-label="Volume" />
<input type="range" data-variant="segmented" value="40" aria-label="Volume" />
```

## Colors

```html
<input
  type="range"
  data-color="primary"
  value="40"
  aria-label="Primary volume"
/>
<input
  type="range"
  data-color="secondary"
  value="40"
  aria-label="Secondary volume"
/>
<input
  type="range"
  data-color="destructive"
  value="40"
  aria-label="Destructive volume"
/>
<input
  type="range"
  data-color="dungeons"
  value="40"
  aria-label="Dungeons volume"
/>
<input
  type="range"
  data-color="legends"
  value="40"
  aria-label="Legends volume"
/>
<input type="range" data-color="realms" value="40" aria-label="Realms volume" />
<input type="range" data-color="gold" value="40" aria-label="Gold volume" />
```

## Orientation

```html
<input
  type="range"
  data-orientation="horizontal"
  value="40"
  aria-label="Horizontal value"
/>
<input
  type="range"
  data-orientation="vertical"
  aria-orientation="vertical"
  value="40"
  aria-label="Vertical value"
/>
```

## Events

The native range input dispatches `input` while its value changes and `change` when the interaction is committed. Read `valueAsNumber` for a numeric value. `name`, form submission, reset, disabled fieldsets, and keyboard behavior use the browser's native implementation.

## API

Slider uses the native `HTMLInputElement` API. OreUI adds these visual data attributes:

| Attribute          | Type                                                                                         | Default        |
| ------------------ | -------------------------------------------------------------------------------------------- | -------------- |
| `data-color`       | `"primary" \| "secondary" \| "destructive" \| "dungeons" \| "legends" \| "realms" \| "gold"` | `"primary"`    |
| `data-orientation` | `"horizontal" \| "vertical"`                                                                 | `"horizontal"` |
| `data-variant`     | `"default" \| "segmented"`                                                                   | `"default"`    |

## CSS variables

| Variable                               | Purpose                   |
| -------------------------------------- | ------------------------- |
| `--ore-slider-unit`                    | Base size unit            |
| `--ore-slider-track-border`            | Track border color        |
| `--ore-slider-active-range-background` | Active range background   |
| `--ore-slider-active-range-highlight`  | Active range highlight    |
| `--ore-slider-track-background`        | Inactive track background |
| `--ore-slider-track-highlight`         | Inactive track highlight  |
| `--ore-slider-thumb-background`        | Thumb background          |
| `--ore-slider-thumb-highlight`         | Thumb highlight           |
| `--ore-slider-thumb-shadow`            | Thumb shadow              |
| `--ore-slider-thumb-border`            | Thumb border color        |

## Accessibility

Add an accessible name with a visible `<label>` or `aria-label`. Slider preserves the native range input's pointer, touch, keyboard, form, disabled, and reset behavior. When using the vertical visual orientation, also set `aria-orientation="vertical"`.
