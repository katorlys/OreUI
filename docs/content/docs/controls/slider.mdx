---
title: Slider
description: Select a numeric value from a range.
---

import { SliderStory } from "@/components/stories/slider.story";

<SliderStory />

## Usage

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/slider";
```

```html
<ore-slider
  name="distance"
  min="2"
  max="32"
  step="1"
  value="12"
  aria-label="Render distance"
></ore-slider>
```

    </Tab>
    <Tab value="React">

```tsx
import { Slider } from "@oreui-web/react/slider";
import { useState } from "react";

export function RenderDistanceSlider() {
  const [value, setValue] = useState(12);

  return (
    <Slider
      aria-label="Render distance"
      min={2}
      max={32}
      step={1}
      value={value}
      onInput={(event) => {
        const slider = event.target as HTMLElement & { value: number };
        setValue(slider.value);
      }}
    />
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ref } from "vue";
import { Slider } from "@oreui-web/vue/slider";

const value = ref(12);
</script>

<template>
  <Slider
    v-model="value"
    name="distance"
    aria-label="Render distance"
    :min="2"
    :max="32"
    :step="1"
  />
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import Slider from "@oreui-web/svelte/slider";

  let value = $state(12);
</script>

<Slider
  bind:value
  name="distance"
  min={2}
  max={32}
  step={1}
  aria-label="Render distance"
/>
```

    </Tab>
    <Tab value="Solid">

```tsx
import { createSignal } from "solid-js";
import { Slider } from "@oreui-web/solid/slider";

export function RenderDistanceSlider() {
  const [value, setValue] = createSignal(12);

  return (
    <Slider
      value={value()}
      onValueChange={setValue}
      name="distance"
      min={2}
      max={32}
      step={1}
      aria-label="Render distance"
    />
  );
}
```

    </Tab>
    <Tab value="Astro">

```astro
<ore-slider name="distance" min="2" max="32" step="1" value="12" aria-label="Render distance"></ore-slider>

<script>
	import "oreui-web/slider";
</script>
```

    </Tab>

</Tabs>

## Variants

```html
<ore-slider value="40" variant="default" aria-label="Volume"></ore-slider>
<ore-slider value="40" variant="segmented" aria-label="Volume"></ore-slider>
```

## Range

Add `range` to select a minimum and maximum value. `value-start` controls the minimum thumb and `value` controls the maximum thumb. The thumbs stop when they meet.

```html
<ore-slider
  range
  min="0"
  max="100"
  value-start="25"
  value="75"
  aria-label-start="Minimum price"
  aria-label="Maximum price"
></ore-slider>
```

## Colors

```html
<ore-slider color="primary" value="40" aria-label="Primary volume"></ore-slider>
<ore-slider
  color="secondary"
  value="40"
  aria-label="Secondary volume"
></ore-slider>
<ore-slider
  color="destructive"
  value="40"
  aria-label="Destructive volume"
></ore-slider>
<ore-slider
  color="dungeons"
  value="40"
  aria-label="Dungeons volume"
></ore-slider>
<ore-slider color="legends" value="40" aria-label="Legends volume"></ore-slider>
<ore-slider color="realms" value="40" aria-label="Realms volume"></ore-slider>
<ore-slider color="gold" value="40" aria-label="Gold volume"></ore-slider>
```

## Orientation

```html
<ore-slider
  orientation="horizontal"
  value="40"
  aria-label="Horizontal value"
></ore-slider>
<ore-slider
  orientation="vertical"
  value="40"
  aria-label="Vertical value"
></ore-slider>
```

## Events

Slider dispatches `input` while its value changes and `change` when the interaction is committed. In range mode, read `valueStart` and `value` from the Slider element. A named range Slider submits both values as a comma-separated string, such as `25,75`.

## API

| Property      | Attribute     | Type                                                                                         | Default        |
| ------------- | ------------- | -------------------------------------------------------------------------------------------- | -------------- |
| `color`       | `color`       | `"primary" \| "secondary" \| "destructive" \| "dungeons" \| "legends" \| "realms" \| "gold"` | `"primary"`    |
| `disabled`    | `disabled`    | `boolean`                                                                                    | `false`        |
| `max`         | `max`         | `number`                                                                                     | `100`          |
| `min`         | `min`         | `number`                                                                                     | `0`            |
| `name`        | `name`        | `string`                                                                                     | `""`           |
| `orientation` | `orientation` | `"horizontal" \| "vertical"`                                                                 | `"horizontal"` |
| `range`       | `range`       | `boolean`                                                                                    | `false`        |
| `step`        | `step`        | `number`                                                                                     | `1`            |
| `value`       | `value`       | `number`                                                                                     | `0`            |
| `valueStart`  | `value-start` | `number`                                                                                     | `0`            |
| `variant`     | `variant`     | `"default" \| "segmented"`                                                                   | `"default"`    |

## CSS variables

| Variable                               | Purpose                   |
| -------------------------------------- | ------------------------- |
| `--ore-slider-unit`                    | Base size unit            |
| `--ore-slider-track-border`            | Track border color        |
| `--ore-slider-active-range-background` | Active range background   |
| `--ore-slider-active-range-highlight`  | Active range highlight    |
| `--ore-slider-track-background`        | Inactive track background |
| `--ore-slider-track-highlight`         | Inactive track highlight  |
| `--ore-slider-thumb-background`        | Thumb background          |
| `--ore-slider-thumb-highlight`         | Thumb highlight           |
| `--ore-slider-thumb-shadow`            | Thumb shadow              |
| `--ore-slider-thumb-border`            | Thumb border color        |

## Accessibility

Add an accessible name with `aria-label`. In range mode, use `aria-label-start` for the minimum thumb and `aria-label` for the maximum thumb. Slider uses native range inputs for pointer and keyboard interaction.
