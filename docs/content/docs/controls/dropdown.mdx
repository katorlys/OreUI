---
title: Dropdown
description: Select an option from a menu.
---

import { DropdownStory } from "@/components/stories/dropdown.story";

<DropdownStory />

## Usage

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/button";
import "oreui-web/dropdown";
```

```html
<ore-dropdown variant="bordered" value="creative">
  <ore-button class="ore-dropdown-trigger" type="button" color="secondary">
    <span class="ore-dropdown-trigger-label">Game mode</span>
  </ore-button>
  <div class="ore-dropdown-menu">
    <button class="ore-dropdown-item" type="button" data-value="survival">
      Survival
    </button>
    <button class="ore-dropdown-item" type="button" data-value="creative">
      Creative
    </button>
    <button class="ore-dropdown-item" type="button" data-value="adventure">
      Adventure
    </button>
    <button
      class="ore-dropdown-item"
      type="button"
      data-value="spectator"
      disabled
    >
      Spectator
    </button>
  </div>
</ore-dropdown>
```

    </Tab>
    <Tab value="React">

```tsx
import { Button } from "@oreui-web/react/button";
import { Dropdown } from "@oreui-web/react/dropdown";
import { useState } from "react";

export function GameModeDropdown() {
  const [value, setValue] = useState("creative");

  return (
    <Dropdown
      value={value}
      variant="bordered"
      onChange={(event) => setValue(event.detail.value)}
    >
      <Button className="ore-dropdown-trigger" type="button" color="secondary">
        <span className="ore-dropdown-trigger-label">Game mode</span>
      </Button>
      <div className="ore-dropdown-menu">
        <button
          className="ore-dropdown-item"
          type="button"
          data-value="survival"
        >
          Survival
        </button>
        <button
          className="ore-dropdown-item"
          type="button"
          data-value="creative"
        >
          Creative
        </button>
        <button
          className="ore-dropdown-item"
          type="button"
          data-value="adventure"
        >
          Adventure
        </button>
      </div>
    </Dropdown>
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ref } from "vue";
import { Button, Dropdown } from "@oreui-web/vue";

const value = ref("creative");
</script>

<template>
  <Dropdown v-model="value" variant="bordered">
    <Button class="ore-dropdown-trigger" type="button" color="secondary">
      <span class="ore-dropdown-trigger-label">Game mode</span>
    </Button>
    <div class="ore-dropdown-menu">
      <button class="ore-dropdown-item" type="button" data-value="survival">
        Survival
      </button>
      <button class="ore-dropdown-item" type="button" data-value="creative">
        Creative
      </button>
      <button class="ore-dropdown-item" type="button" data-value="adventure">
        Adventure
      </button>
      <button
        class="ore-dropdown-item"
        type="button"
        data-value="spectator"
        disabled
      >
        Spectator
      </button>
    </div>
  </Dropdown>
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import Button from "@oreui-web/svelte/button";
  import Dropdown from "@oreui-web/svelte/dropdown";

  let value = $state("creative");
</script>

<Dropdown bind:value variant="bordered">
  <Button class="ore-dropdown-trigger" type="button" color="secondary">
    <span class="ore-dropdown-trigger-label">Game mode</span>
  </Button>
  <div class="ore-dropdown-menu">
    <button class="ore-dropdown-item" type="button" data-value="survival">Survival</button>
    <button class="ore-dropdown-item" type="button" data-value="creative">Creative</button>
    <button class="ore-dropdown-item" type="button" data-value="adventure">Adventure</button>
  </div>
</Dropdown>
```

    </Tab>
    <Tab value="Astro">

```astro
<ore-dropdown variant="bordered" value="creative">
  <ore-button class="ore-dropdown-trigger" type="button" color="secondary">
		<span class="ore-dropdown-trigger-label">Game mode</span>
	</ore-button>
	<div class="ore-dropdown-menu">
		<button class="ore-dropdown-item" type="button" data-value="survival">Survival</button>
		<button class="ore-dropdown-item" type="button" data-value="creative">Creative</button>
		<button class="ore-dropdown-item" type="button" data-value="adventure">Adventure</button>
	</div>
</ore-dropdown>

<script>
	import "oreui-web/button";
	import "oreui-web/dropdown";
</script>
```

    </Tab>

</Tabs>

## Variants

`borderless` shows item borders on interaction, while `bordered` keeps them visible.

## Events

| Event         | Detail                                 |
| ------------- | -------------------------------------- |
| `change`      | `{ item: HTMLElement; value: string }` |
| `open-change` | `boolean`                              |

In React, use `onChange` and `onOpenChange`.

## API

| Property      | Attribute      | Type                         | Default        |
| ------------- | -------------- | ---------------------------- | -------------- |
| `defaultOpen` | `default-open` | `boolean`                    | `false`        |
| `open`        | `open`         | `boolean`                    | `false`        |
| `value`       | `value`        | `string`                     | `""`           |
| `variant`     | `variant`      | `"bordered" \| "borderless"` | `"borderless"` |

## Public classes

| Class                         | Purpose                             |
| ----------------------------- | ----------------------------------- |
| `.ore-dropdown-trigger`       | Button that opens the menu          |
| `.ore-dropdown-trigger-label` | Text updated from the selected item |
| `.ore-dropdown-menu`          | Popover menu                        |
| `.ore-dropdown-item`          | Selectable menu item                |

## CSS variables

The menu's scrollable area also accepts all [Scrollbar CSS variables](../../navigation/scrollbar#css-variables).

| Variable                               | Purpose                        |
| -------------------------------------- | ------------------------------ |
| `--ore-dropdown-unit`                  | Base size unit                 |
| `--ore-dropdown-menu-background`       | Menu background color          |
| `--ore-dropdown-menu-outline`          | Menu outline color             |
| `--ore-dropdown-item-background`       | Item background                |
| `--ore-dropdown-item-background-hover` | Hovered item background        |
| `--ore-dropdown-item-border`           | Divider color between items    |
| `--ore-dropdown-foreground`            | Item text and icon color       |
| `--ore-dropdown-visible-items`         | Items visible before scrolling |

## Accessibility

Dropdown manages menu roles, selection state, focus, and `Arrow`, `Home`, `End`, `Enter`, `Space`, `Esc`, and `Tab` keys.  
Use native `disabled` on unavailable items.
