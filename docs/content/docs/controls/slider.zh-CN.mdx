---
title: Slider 滑块
description: 在范围内用滑块选取数值
---

import { SliderStory } from "@/components/stories/slider.story";

<SliderStory />

## 用法

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/slider";
```

```html
<input
  class="ore-slider"
  type="range"
  name="distance"
  min="2"
  max="32"
  step="1"
  value="12"
  aria-label="Render distance"
/>
```

    </Tab>
    <Tab value="React">

```tsx
import { Slider } from "@oreui-web/react/slider";
import { useState } from "react";

export function RenderDistanceSlider() {
  const [value, setValue] = useState(12);

  return (
    <Slider
      aria-label="Render distance"
      min={2}
      max={32}
      step={1}
      value={value}
      onInput={(event) => {
        setValue(event.currentTarget.valueAsNumber);
      }}
    />
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ref } from "vue";
import { Slider } from "@oreui-web/vue/slider";
const value = ref(12);
</script>

<template>
  <Slider
    v-model="value"
    name="distance"
    aria-label="Render distance"
    :min="2"
    :max="32"
    :step="1"
  />
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import Slider from "@oreui-web/svelte/slider";

  let value = $state(12);
</script>

<Slider
  bind:value
  name="distance"
  min={2}
  max={32}
  step={1}
  aria-label="Render distance"
/>
```

    </Tab>
    <Tab value="Solid">

```tsx
import { createSignal } from "solid-js";
import { Slider } from "@oreui-web/solid/slider";

export function RenderDistanceSlider() {
  const [value, setValue] = createSignal(12);

  return (
    <Slider
      value={value()}
      onValueChange={setValue}
      name="distance"
      min={2}
      max={32}
      step={1}
      aria-label="Render distance"
    />
  );
}
```

    </Tab>
    <Tab value="Astro">

```astro
<input type="range" name="distance" min="2" max="32" step="1" value="12" aria-label="Render distance" />

<script>
	import "oreui-web/slider";
</script>
```

    </Tab>

</Tabs>

## 变体

```html
<input type="range" value="40" aria-label="Volume" />
<input type="range" data-variant="segmented" value="40" aria-label="Volume" />
```

## 颜色

```html
<input
  type="range"
  data-color="primary"
  value="40"
  aria-label="Primary volume"
/>
<input
  type="range"
  data-color="secondary"
  value="40"
  aria-label="Secondary volume"
/>
<input
  type="range"
  data-color="destructive"
  value="40"
  aria-label="Destructive volume"
/>
<input
  type="range"
  data-color="dungeons"
  value="40"
  aria-label="Dungeons volume"
/>
<input
  type="range"
  data-color="legends"
  value="40"
  aria-label="Legends volume"
/>
<input type="range" data-color="realms" value="40" aria-label="Realms volume" />
<input type="range" data-color="gold" value="40" aria-label="Gold volume" />
```

## 方向

```html
<input
  type="range"
  data-orientation="horizontal"
  value="40"
  aria-label="Horizontal value"
/>
<input
  type="range"
  data-orientation="vertical"
  aria-orientation="vertical"
  value="40"
  aria-label="Vertical value"
/>
```

## 事件

原生 range input 的值变化时会触发 `input` 事件，提交交互时会触发 `change` 事件。使用 `valueAsNumber` 读取数值。`name`、表单提交、重置、禁用 fieldset 和键盘行为均由浏览器原生实现。

## API

Slider 使用原生 `HTMLInputElement` API。OreUI 只增加以下视觉 data 属性：

| 属性               | 类型                                                                                         | 默认值         |
| ------------------ | -------------------------------------------------------------------------------------------- | -------------- |
| `data-color`       | `"primary" \| "secondary" \| "destructive" \| "dungeons" \| "legends" \| "realms" \| "gold"` | `"primary"`    |
| `data-orientation` | `"horizontal" \| "vertical"`                                                                 | `"horizontal"` |
| `data-variant`     | `"default" \| "segmented"`                                                                   | `"default"`    |

## CSS 变量

| 变量                                   | 用途           |
| -------------------------------------- | -------------- |
| `--ore-slider-unit`                    | 基础尺寸单位   |
| `--ore-slider-track-border`            | 轨道边框颜色   |
| `--ore-slider-active-range-background` | 激活范围背景   |
| `--ore-slider-active-range-highlight`  | 激活范围高光   |
| `--ore-slider-track-background`        | 未激活轨道背景 |
| `--ore-slider-track-highlight`         | 未激活轨道高光 |
| `--ore-slider-thumb-background`        | 滑块背景       |
| `--ore-slider-thumb-highlight`         | 滑块高光       |
| `--ore-slider-thumb-shadow`            | 滑块阴影       |
| `--ore-slider-thumb-border`            | 滑块边框颜色   |

## 无障碍

使用可见 `<label>` 或 `aria-label` 设置无障碍名称。Slider 保留原生 range input 的指针、触摸、键盘、表单、禁用和重置行为。使用竖直视觉方向时，还应设置 `aria-orientation="vertical"`。
