---
title: Checkbox
description: Tick an option.
---

import { CheckboxStory } from "@/components/stories/checkbox.story";

<CheckboxStory />

## Usage

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/styles.css";
```

```html
<label>
  <input class="ore-checkbox" type="checkbox" name="updates" value="yes" />
  Receive updates
</label>
```

    </Tab>
    <Tab value="React">

```tsx
import { Checkbox } from "@oreui-web/react/checkbox";
import { useState } from "react";

export function UpdatesCheckbox() {
  const [checked, setChecked] = useState(false);

  return (
    <Checkbox
      checked={checked}
      name="updates"
      value="yes"
      onChange={(event) => {
        setChecked(event.currentTarget.checked);
      }}
    >
      Receive updates
    </Checkbox>
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ref } from "vue";
import { Checkbox } from "@oreui-web/vue/checkbox";

const checked = ref(false);
</script>

<template>
  <Checkbox v-model="checked" name="updates" value="yes">
    Receive updates
  </Checkbox>
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import Checkbox from "@oreui-web/svelte/checkbox";

  let checked = $state(false);
</script>

<Checkbox bind:checked name="updates" value="yes">
  Receive updates
</Checkbox>
```

    </Tab>
    <Tab value="Solid">

```tsx
import { createSignal } from "solid-js";
import { Checkbox } from "@oreui-web/solid/checkbox";

export function UpdatesCheckbox() {
  const [checked, setChecked] = createSignal(false);

  return (
    <Checkbox
      checked={checked()}
      onCheckedChange={setChecked}
      name="updates"
      value="yes"
    >
      Receive updates
    </Checkbox>
  );
}
```

    </Tab>
    <Tab value="Astro">

```astro
<label>
  <input class="ore-checkbox" type="checkbox" name="updates" value="yes" />
  Receive updates
</label>

<style is:global>
  @import "oreui-web/styles.css";
</style>
```

    </Tab>

</Tabs>

## Colors

```html
<label
  ><input
    class="ore-checkbox"
    type="checkbox"
    data-color="primary"
    checked
  />Primary</label
>
<label
  ><input
    class="ore-checkbox"
    type="checkbox"
    data-color="secondary"
    checked
  />Secondary</label
>
<label
  ><input
    class="ore-checkbox"
    type="checkbox"
    data-color="destructive"
    checked
  />Destructive</label
>
<label
  ><input
    class="ore-checkbox"
    type="checkbox"
    data-color="dungeons"
    checked
  />Dungeons</label
>
<label
  ><input
    class="ore-checkbox"
    type="checkbox"
    data-color="legends"
    checked
  />Legends</label
>
<label
  ><input
    class="ore-checkbox"
    type="checkbox"
    data-color="realms"
    checked
  />Realms</label
>
<label
  ><input
    class="ore-checkbox"
    type="checkbox"
    data-color="gold"
    checked
  />Gold</label
>
```

## States

```html
<label><input class="ore-checkbox" type="checkbox" checked />Enabled</label>
<label
  ><input class="ore-checkbox" type="checkbox" disabled />Unavailable</label
>
<label
  ><input class="ore-checkbox" type="checkbox" required />Required
  agreement</label
>
```

## Forms

Checked values are included in `FormData`. Reset restores the initial `checked` state.

```html
<form>
  <label>
    <input
      class="ore-checkbox"
      type="checkbox"
      name="terms"
      value="accepted"
      required
    />
    Accept the terms
  </label>
  <button type="submit">Continue</button>
  <button type="reset" data-color="secondary">Reset</button>
</form>
```

## Events

The native checkbox dispatches `input` and `change` events when its value changes.

## API

Checkbox uses the native `HTMLInputElement` API. Use `checked`, `disabled`, `name`, `required`, and `value` directly; use `data-color` only for the OreUI palette. Native methods such as `checkValidity()` and `reportValidity()` remain available.

## CSS variables

| Variable                                   | Purpose                    |
| ------------------------------------------ | -------------------------- |
| `--ore-checkbox-unit`                      | Base size unit             |
| `--ore-checkbox-background`                | Unchecked background       |
| `--ore-checkbox-highlight`                 | Unchecked highlight        |
| `--ore-checkbox-border`                    | Border color               |
| `--ore-checkbox-indicator`                 | Checkmark color            |
| `--ore-checkbox-active-background`         | Checked background         |
| `--ore-checkbox-active-background-hover`   | Checked hover background   |
| `--ore-checkbox-active-background-pressed` | Checked pressed background |
| `--ore-checkbox-active-highlight`          | Checked highlight          |
| `--ore-checkbox-active-highlight-hover`    | Checked hover highlight    |
| `--ore-checkbox-active-highlight-pressed`  | Checked pressed highlight  |

## Accessibility

The native input provides checkbox semantics, label activation, Space key behavior, form participation, validation, and reset behavior without JavaScript.
