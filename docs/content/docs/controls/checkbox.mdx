---
title: Checkbox
description: Tick an option.
---

import { CheckboxStory } from "@/components/stories/checkbox.story";

<CheckboxStory />

## Usage

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/checkbox";
```

```html
<ore-checkbox name="updates" value="yes"> Receive updates </ore-checkbox>
```

    </Tab>
    <Tab value="React">

```tsx
import { Checkbox } from "@oreui-web/react/checkbox";
import { useState } from "react";

export function UpdatesCheckbox() {
  const [checked, setChecked] = useState(false);

  return (
    <Checkbox
      checked={checked}
      name="updates"
      value="yes"
      onChange={(event) => {
        const checkbox = event.target as HTMLElement & { checked: boolean };
        setChecked(checkbox.checked);
      }}
    >
      Receive updates
    </Checkbox>
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { ref } from "vue";
import { Checkbox } from "@oreui-web/vue/checkbox";

const checked = ref(false);
</script>

<template>
  <Checkbox v-model="checked" name="updates" value="yes">
    Receive updates
  </Checkbox>
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import Checkbox from "@oreui-web/svelte/checkbox";

  let checked = $state(false);
</script>

<Checkbox bind:checked name="updates" value="yes">
  Receive updates
</Checkbox>
```

    </Tab>
    <Tab value="Solid">

```tsx
import { createSignal } from "solid-js";
import { Checkbox } from "@oreui-web/solid/checkbox";

export function UpdatesCheckbox() {
  const [checked, setChecked] = createSignal(false);

  return (
    <Checkbox
      checked={checked()}
      onCheckedChange={setChecked}
      name="updates"
      value="yes"
    >
      Receive updates
    </Checkbox>
  );
}
```

    </Tab>
    <Tab value="Astro">

```astro
<ore-checkbox name="updates" value="yes">
	Receive updates
</ore-checkbox>

<script>
	import "oreui-web/checkbox";
</script>
```

    </Tab>

</Tabs>

## Colors

```html
<ore-checkbox color="primary" checked>Primary</ore-checkbox>
<ore-checkbox color="secondary" checked>Secondary</ore-checkbox>
<ore-checkbox color="destructive" checked>Destructive</ore-checkbox>
<ore-checkbox color="dungeons" checked>Dungeons</ore-checkbox>
<ore-checkbox color="legends" checked>Legends</ore-checkbox>
<ore-checkbox color="realms" checked>Realms</ore-checkbox>
<ore-checkbox color="gold" checked>Gold</ore-checkbox>
```

## States

```html
<ore-checkbox checked>Enabled</ore-checkbox>
<ore-checkbox disabled>Unavailable</ore-checkbox>
<ore-checkbox required>Required agreement</ore-checkbox>
```

## Forms

Checked values are included in `FormData`. Reset restores the initial `checked` state.

```html
<form>
  <ore-checkbox name="terms" value="accepted" required>
    Accept the terms
  </ore-checkbox>
  <ore-button type="submit">Continue</ore-button>
  <ore-button type="reset" color="secondary">Reset</ore-button>
</form>
```

## Events

Checkbox dispatches native `input` and `change` events after its value changes.

## API

| Property   | Attribute  | Type                                                                                         | Default     |
| ---------- | ---------- | -------------------------------------------------------------------------------------------- | ----------- |
| `checked`  | `checked`  | `boolean`                                                                                    | `false`     |
| `color`    | `color`    | `"primary" \| "secondary" \| "destructive" \| "dungeons" \| "legends" \| "realms" \| "gold"` | `"primary"` |
| `disabled` | `disabled` | `boolean`                                                                                    | `false`     |
| `name`     | `name`     | `string`                                                                                     | `""`        |
| `required` | `required` | `boolean`                                                                                    | `false`     |
| `value`    | `value`    | `string`                                                                                     | `"on"`      |

| Method             | Description                        |
| ------------------ | ---------------------------------- |
| `checkValidity()`  | Check the current validity         |
| `reportValidity()` | Check validity and report an error |

## CSS variables

| Variable                                   | Purpose                    |
| ------------------------------------------ | -------------------------- |
| `--ore-checkbox-unit`                      | Base size unit             |
| `--ore-checkbox-background`                | Unchecked background       |
| `--ore-checkbox-highlight`                 | Unchecked highlight        |
| `--ore-checkbox-border`                    | Border color               |
| `--ore-checkbox-indicator`                 | Checkmark color            |
| `--ore-checkbox-active-background`         | Checked background         |
| `--ore-checkbox-active-background-hover`   | Checked hover background   |
| `--ore-checkbox-active-background-pressed` | Checked pressed background |
| `--ore-checkbox-active-highlight`          | Checked highlight          |
| `--ore-checkbox-active-highlight-hover`    | Checked hover highlight    |
| `--ore-checkbox-active-highlight-pressed`  | Checked pressed highlight  |

## Accessibility

Checkbox provides checkbox semantics and supports Space.
