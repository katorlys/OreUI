---
title: Radio
description: The base component for Radio Group.
---

## Usage

See [Radio Group](../radio-group).

## Colors

```html
<label><input type="radio" data-color="primary" checked /> Primary</label>
<label><input type="radio" data-color="secondary" checked /> Secondary</label>
<label
  ><input type="radio" data-color="destructive" checked /> Destructive</label
>
<label><input type="radio" data-color="dungeons" checked /> Dungeons</label>
<label><input type="radio" data-color="legends" checked /> Legends</label>
<label><input type="radio" data-color="realms" checked /> Realms</label>
<label><input type="radio" data-color="gold" checked /> Gold</label>
```

## States

```html
<label>
  <input type="radio" name="mode" value="survival" checked />
  Survival
</label>
<label>
  <input type="radio" name="mode" value="hardcore" disabled />
  Hardcore
</label>
```

Radios with the same `name` and form are mutually exclusive.

## Events

Selecting a new option dispatches native `input` and `change` events.

## Attributes

Radio uses the native input attributes `checked`, `disabled`, `name`, `required`, and `value`. Use `data-color` for the OreUI color.

## CSS variables

| Variable                                | Purpose                     |
| --------------------------------------- | --------------------------- |
| `--ore-radio-unit`                      | Base size unit              |
| `--ore-radio-background`                | Unselected background       |
| `--ore-radio-highlight`                 | Unselected highlight        |
| `--ore-radio-border`                    | Border color                |
| `--ore-radio-indicator-light`           | Indicator light color       |
| `--ore-radio-indicator-dim`             | Indicator midtone color     |
| `--ore-radio-indicator-dark`            | Indicator dark color        |
| `--ore-radio-active-background`         | Selected background         |
| `--ore-radio-active-background-hover`   | Selected hover background   |
| `--ore-radio-active-background-pressed` | Selected pressed background |
| `--ore-radio-active-highlight`          | Selected highlight          |
| `--ore-radio-active-highlight-hover`    | Selected hover highlight    |
| `--ore-radio-active-highlight-pressed`  | Selected pressed highlight  |

## Accessibility

The native radio input provides radio semantics, form behavior, and validation. Use arrow keys to move within a same-name group and `Space` to select the focused option.
