---
title: Radio
description: Select one option in a radio group.
---

import { RadioStory } from "@/components/stories/radio.story";

<RadioStory />

## Usage
<Tabs groupId="framework" items={["Vanilla", "React", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "@katorlys/oreui/radio";
```

```html
<ore-radio name="mode" value="creative">Creative</ore-radio>
```
	</Tab>
	<Tab value="React">
```tsx
import { OreRadio } from "@katorlys/oreui-react/radio";

<OreRadio name="mode" value="creative">Creative</OreRadio>
```
	</Tab>
	<Tab value="Astro">
```astro
<ore-radio name="mode" value="creative">Creative</ore-radio>

<script>
	import "@katorlys/oreui/radio";
</script>
```
	</Tab>
</Tabs>

## States
```html
<ore-radio name="mode" value="survival" checked>Survival</ore-radio>
<ore-radio name="mode" value="hardcore" disabled>Hardcore</ore-radio>
```

Radios with the same `name` and form are mutually exclusive.

## Events
Selecting a new option dispatches native `input` and `change` events.

## API
|Property|Attribute|Type|Default|
|---|---|---|---|
|`checked`|`checked`|`boolean`|`false`|
|`disabled`|`disabled`|`boolean`|`false`|
|`name`|`name`|`string`|`""`|
|`required`|`required`|`boolean`|`false`|
|`value`|`value`|`string`|`"on"`|

## Accessibility
Radio provides radio semantics. Use `Arrow keys` to move within a named group and `Space` to select the focused option.