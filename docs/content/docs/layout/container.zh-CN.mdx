---
title: Container 容器
description: 在容器中组合内容
---

import { ContainerStory } from "@/components/stories/container.story";

<ContainerStory />

## 用法

<Tabs groupId="framework" items={["Vanilla", "React", "Vue", "Svelte", "Solid", "Astro"]} persist>
	<Tab value="Vanilla">
```js
import "oreui-web/container";
```

```html
<div class="ore-container" data-variant="dark">
  <h2>World settings</h2>
  <p>Choose how players join your world.</p>
</div>
```

    </Tab>
    <Tab value="React">

```tsx
import { Container } from "@oreui-web/react/container";

export function WorldSettings() {
  return (
    <Container variant="dark">
      <h2>World settings</h2>
      <p>Choose how players join your world.</p>
    </Container>
  );
}
```

    </Tab>
    <Tab value="Vue">

```vue
<script setup lang="ts">
import { Container } from "@oreui-web/vue/container";
</script>

<template>
  <Container variant="dark"
    ><h2>World settings</h2>
    <p>Choose how players join your world.</p></Container
  >
</template>
```

    </Tab>
    <Tab value="Svelte">

```svelte
<script lang="ts">
  import Container from "@oreui-web/svelte/container";
</script>

<Container variant="dark">
  <h2>World settings</h2>
  <p>Choose how players join your world.</p>
</Container>
```

    </Tab>
    <Tab value="Solid">

```tsx
import { Container } from "@oreui-web/solid/container";

export function Panel() {
  return <Container variant="dark">World settings</Container>;
}
```

    </Tab>
    <Tab value="Astro">

```astro
<div class="ore-container" data-variant="dark">
	<h2>World settings</h2>
	<p>Choose how players join your world.</p>
</div>

<script>
	import "oreui-web/container";
</script>
```

    </Tab>

</Tabs>

## 变体

```html
<div class="ore-container" data-variant="dark">Dark surface</div>
<div class="ore-container" data-variant="light">Light surface</div>
```

## API

| Prop      | HTML 属性      | 类型                 | 默认值   |
| --------- | -------------- | -------------------- | -------- |
| `variant` | `data-variant` | `"dark" \| "light"` | `"dark"` |

## CSS 变量

| 变量                               | 用途                     |
| ---------------------------------- | ------------------------ |
| `--ore-container-unit`             | 基础尺寸单位             |
| `--ore-container-dark-background`  | 深色变体背景颜色         |
| `--ore-container-light-background` | 浅色变体背景颜色         |
| `--ore-container-highlight`        | 顶部和侧边高光颜色       |
| `--ore-container-shadow`           | 底部阴影颜色             |
| `--ore-container-divider`          | 区块之间的分隔线颜色     |
| `--ore-container-foreground`       | 内容文本颜色             |
| `--ore-container-padding`          | 表面边缘与内容之间的间距 |
| `--ore-container-gap`              | 直接子元素之间的间距     |
